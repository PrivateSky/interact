function ThreadPowerCord(energySource) { // seed or array of constitution bundle paths

    function connectToEnergy() {
    } // constructor

    this.startSwarm = function (swarmSerialization) {
    };
}

module.exports = ThreadPowerCord;
